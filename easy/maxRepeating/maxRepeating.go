package maxrepeating

// undone -> Come Back in a month
// func MaxRepeating(sequence string, word string) int {
// 	prevIndex := 0
// 	for i := len(word) - 1; i < len(sequence); i++ {
// 		if sequence[prevIndex:i] == word {

// 		}
// 	}
// }
